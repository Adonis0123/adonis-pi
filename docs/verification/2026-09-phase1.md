# 第一阶段验收记录（2026-09-22）

执行方式：Native，本会话按 plan 逐任务实现。全部自动化测试：`npm test` 69 通过，`npm run typecheck` 通过。验收在主仓（`ai-video-collection`）以 `pin 1` 启动；非交互步骤用 `-p` print 模式跑，交互步骤留给用户。

| 编号 | 结果 | 备注（模型、时间、偏差） |
|---|---|---|
| V1 | `UNVERIFIED` | ChatGPT OAuth 需要交互式 `/login`，`auth.json` 仍为空，本会话不能完成。GLM 已在主仓完成同样的只读问答（见 V6）。13:00 tmux 内 `pin 1` 在主仓启动时 project-trust 提示如期出现（5 个选项），本轮选 `Trust (this session only)`。 |
| V2 | `UNVERIFIED` | 需要用户挑一个真实小需求；在用户仓库改代码不在本会话授权内。 |
| V3 | 部分通过 | 12:57，tmux 内 `pin 1 --model glm/glm-5.3`：模型按提示调用 `AskUserQuestion`（header `Color`，选项 Red/Blue 带 description）。pi-tui 自定义面板渲染正常：标题 `Color · (1/1)`、选项与说明、`Other (type your own)`、按键提示；`↓` `Enter` 选 Blue 后工具结果 `Q1 Color: Blue`，模型回复 `You chose Blue`。`/skill:coco-commit` 全流程仍 `UNVERIFIED`（会在用户仓提交）。 |
| V4 | 通过 | 12:00 print 模式：`git push --force --dry-run origin HEAD` 被拦，agent 原文回报 deny 正则。12:59 TUI：同一命令弹出 `Permission gate` 确认框（正则、命令、`Yes/No`），选 No 后模型回报 `User denied: command matches denyCommands …`，命令未执行。 |
| V5 | 通过 | 12:57:13 从 TUI 内部由 ask-user-question 发出 question 事件，`notify.log` 12:57:16 `relay pi/confirm -> feishu:<群>`，卡片进群话题；回答后 `tool_result` 的 `--mark` 使 12:58:16 `resolved pi/confirm after 63s` 并 relay `✅ 已处理`。12:59:22 权限门发出 PermissionRequest（`events.log` 有记录），`notify.log` 没有新的 relay 行，13:00:22 按同一条消息 id 标记已处理；这是 Notifier 对同一 session 的复用行为，属 Notifier 侧观察，未深究。11:56 的冒烟卡片 13:01 由 `--mark` 手动标记已处理。Notifier 自身测试新增 7 条 `--agent pi` 用例已在 `~/.claude` 单独提交。用户是否看到卡片待确认。 |
| V6 | 通过（两处修正） | GLM 11:57：`glm/glm-5.3` 一次 `read` 工具往返，答出 `"test": "vitest"`。Kimi 第一次 404：`baseUrl` 带 `/v1` 会被 pi 再追加 `/v1/messages`，改为 `https://api.kimi.com/coding`；第二次 401：`k3-1m` 被 coding 端点以 `recognized as other:k3-1m, please set model id as k3` 拒绝（非 Claude 客户端只认 `k3`）。默认模型改为 `k3` 后 12:03 通过，一次 `read` 往返，答案相同。模板与账号层 `models.json` 同步修改。 |
| V7 | 通过 | `pin doctor 1` 全 OK 退出 0；`chmod 644 proxy.env` 后报 `FAIL proxy.env mode is 644, must be 600` 退出 1；恢复 600 后再次全 OK。 |
| V8 | 通过 | `scripts/leak-check.sh`：植入 `xoxb-…` 假凭证被报出（退出 1）；`proxy.env.local` 被 `.gitignore` 忽略；实跑 `leak-check: clean`。 |

补充冒烟：`pin 1 --no-skills --model glm/glm-5.3 -p "Reply with exactly: ok"` 退出 0、输出 `ok`，说明 `settings.json` 的 `packages` 路径加载了本包四个 extension，没有加载错误。

## TUI 验收补充（13:00，tmux 驱动）

- 启动方式：`tmux` 新会话中 `pin 1 --model glm/glm-5.3`，按键与抓屏都由脚本完成，验证的是真实 TUI 路径。
- pi 载入 `~/.agents/skills` 时报了 7 条 skill 警告：`skill-creator`、`ui-ux-pro-max` 名字冲突（project 与 user 各一份，pi 取一份跳过另一份）；`admin-web`、`microservice-template`、`claude-api` 的 description 超 1024 字符；`google-doc-static-gen` frontmatter YAML 无法解析；`skill-development` 名字含非法字符。这些 skill 在 pi 里不可用，Claude Code 不做这些校验。第二阶段候选：`!pattern` 排除或修 frontmatter。
- pi 提示 `tmux extended-keys is off`，只影响 tmux 里的修饰键，真实终端无此问题。

## 公开仓与 clean-clone（13:10）

- 用户手动执行 `gh repo create Adonis0123/adonis-pi --public --source=. --push`（宿主拦下了由 agent 发起的这一步），仓库 https://github.com/Adonis0123/adonis-pi，分支 `main`。
- 全新 clone 到临时目录：13 个 commit；`npm ci` 成功；`npm test` 69 通过；`npm run typecheck` 通过；`npm run leak-check` 输出 `leak-check: clean`；`PIN_DRY_RUN=1 ./bin/pin 1` 解析到账号目录。plan Task 10 Step 5 完成。

## 审查与重构（2026-09-22 13:30–14:20）

代码审查（`ocr delegate`，22 个可审文件全部读完）加子代理架构走查后，同一会话完成修复与重构，pi 本体未改。

- **修掉的 Medium**：权限门对 `&`、`(`、`$(`、`if/then/do/time` 前缀与 `"$HOME"`/`${HOME}` 的绕过（探针 8 条全部拦下）；`adonis-pi.json` 嵌套拼错静默通过；账号数组整体覆盖模板；startup-check 漏报 `$ADONIS_PI_NOTIFY_CMD`；RPC 模式提醒策略不一致（现在统一为 RPC 不发）。
- **架构（4 个 Strong 全部采纳）**：`lib/session.ts` 取代 `runtime-config`，Surface 一张表、Notifier 门控一处、配置随文件变化重读；`bin/lib/account.ts` 接管 setup/doctor 并直接复用 `lib/config.ts`，`pin` 只剩 source 与 exec，三个 mjs 删除；Ask 面板改为 `createPanel` 可注入 editor/theme 单测；`AskCtx` 类型消掉 `as any`。
- **测试**：69 → 87 条，新增 `session.test.ts`、`ask-panel.test.ts`；extension 测试改为注入假 spawn，不再轮询 shell fixture；`typecheck`、`leak-check` 通过。
- **真机复验**：`pin doctor 1` 全 OK（新增 `adonis-pi.json is valid`、`$ADONIS_PI_NOTIFY_CMD provided` 两行）；`pin 1 -p` GLM 回 `ok`；print 模式 `git push --force --dry-run` 仍被拦并原文回报；tmux 内真实 TUI 走 Other 自由输入：选 Other → 编辑框出现 → 键入 `purple` → Enter → 模型回 `You chose purple.`。
- **doctor 新观察**：pi 自己往 `settings.json` 写 `lastChangelogVersion`，drift 现已忽略该键。

## Kimi provider 收敛（2026-09-22 15:00）

`/model` 列表出现两组 Kimi：我们 `models.json` 手写的 `kimi` 与 pi 内建的 `kimi-coding`（同一端点 `https://api.kimi.com/coding`，`k3` 上下文 1048576）。按用户决定删除手写版，模板与账号层同步；`proxy.env` 仍需 `KIMI_API_KEY`，由内建 provider 读取。真实 TUI 以 `kimi-coding/k3` 为默认启动并回复正常；`pin` dry-run 现在也报告 `proxy.env` 导出的变量名。V6 里关于 `baseUrl` 与 `k3-1m` 的记录保留为历史。

## UNVERIFIED 清单

- V1 `/login`；V2；V3 的 `/skill:coco-commit` 全流程；用户是否收到飞书卡片。
- `k3`、`k3-256k`、`kimi-for-coding` 在 coding 端点的实际上下文长度（模板按官方 K3 文档填 1M / 256K / 256K）。
- 第一阶段不算完成，直到 V1、V2、V3 交互步骤由用户走完。
