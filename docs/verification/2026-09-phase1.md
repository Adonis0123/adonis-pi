# 第一阶段验收记录（2026-09-22）

执行方式：Native，本会话按 plan 逐任务实现。全部自动化测试：`npm test` 69 通过，`npm run typecheck` 通过。验收在主仓（`ai-video-collection`）以 `pin 1` 启动；非交互步骤用 `-p` print 模式跑，交互步骤留给用户。

| 编号 | 结果 | 备注（模型、时间、偏差） |
|---|---|---|
| V1 | `UNVERIFIED` | ChatGPT OAuth 需要交互式 `/login`，本会话不能完成。GLM 已在主仓完成同样的只读问答（见 V6）。主仓含 `.agents/`，首次 TUI 启动会弹 project-trust 提示。 |
| V2 | `UNVERIFIED` | 需要用户挑一个真实小需求，交互完成。 |
| V3 | `UNVERIFIED` | `/skill:coco-commit` 与 `AskUserQuestion` 面板需要 TUI。工具的 13 条单元测试通过；面板按键逻辑（reducer）有测试，pi-tui 渲染没有。 |
| V4 | 通过（print 模式变体） | 12:00，`glm/glm-5.3`，`pin 1 -p`：`git push --force --dry-run origin HEAD` 被拦，agent 原文回报 `adonis-pi permission gate: command matches denyCommands /^\s*git\s+…/`，命令未执行。TUI 下的 confirm 对话框 `UNVERIFIED`。 |
| V5 | 部分通过 | 11:56，用 Notifier 脚本 `--agent pi` 真实发送一条 confirm/question 冒烟；Notifier 运行目录出现 `pi-pi-smoke-confirm.msg / .pending / .thread`，与其他宿主的成功发送一致。`notify.log` 记到 `relay pi/confirm -> feishu:<群> | 🟠 pi 需要你确认`，卡片已进飞书群话题；用户看到与否待确认。Notifier 自身测试新增 7 条 `--agent pi` 用例，全部通过。从 pi TUI 内部触发 `UNVERIFIED`（attention-notify 在非 TUI 模式不发）。 |
| V6 | 通过（两处修正） | GLM 11:57：`glm/glm-5.3` 一次 `read` 工具往返，答出 `"test": "vitest"`。Kimi 第一次 404：`baseUrl` 带 `/v1` 会被 pi 再追加 `/v1/messages`，改为 `https://api.kimi.com/coding`；第二次 401：`k3-1m` 被 coding 端点以 `recognized as other:k3-1m, please set model id as k3` 拒绝（非 Claude 客户端只认 `k3`）。默认模型改为 `k3` 后 12:03 通过，一次 `read` 往返，答案相同。模板与账号层 `models.json` 同步修改。 |
| V7 | 通过 | `pin doctor 1` 全 OK 退出 0；`chmod 644 proxy.env` 后报 `FAIL proxy.env mode is 644, must be 600` 退出 1；恢复 600 后再次全 OK。 |
| V8 | 通过 | `scripts/leak-check.sh`：植入 `xoxb-…` 假凭证被报出（退出 1）；`proxy.env.local` 被 `.gitignore` 忽略；实跑 `leak-check: clean`。 |

补充冒烟：`pin 1 --no-skills --model glm/glm-5.3 -p "Reply with exactly: ok"` 退出 0、输出 `ok`，说明 `settings.json` 的 `packages` 路径加载了本包四个 extension，没有加载错误。

## UNVERIFIED 清单

- V1、V2、V3 全部；V4 的 TUI confirm 对话框；V5 从 pi 内部触发；用户是否收到 11:56 的飞书冒烟卡片。
- `k3`、`k3-256k`、`kimi-for-coding` 在 coding 端点的实际上下文长度（模板按官方 K3 文档填 1M / 256K / 256K）。
- 第一阶段不算完成，直到 V1–V3、V5 交互步骤由用户走完。
